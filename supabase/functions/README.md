# Supabase Edge Functions

Planned functions:

- `terra-webhook`: receives Terra webhook events, validates signature, upserts biometrics.
- `terra-connect`: creates Terra user mapping + connect URL for authenticated app user.
- `terra-status`: returns current Terra connection status for authenticated app user.
- `admin-metrics`: restricted trial aggregate metrics for admin portal.
